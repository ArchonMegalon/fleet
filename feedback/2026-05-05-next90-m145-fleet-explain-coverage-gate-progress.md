Package: `next90-m145-fleet-explain-coverage-gate`
Frontier: `1456045606`

- Added `scripts/materialize_next90_m145_fleet_explain_coverage_gate.py` and `scripts/verify_next90_m145_fleet_explain_coverage_gate.py` so Fleet now materializes one successor-wave packet that checks canonical queue/registry truth plus sibling explain proof for core packets, desktop drawer follow-up, mobile quick explain, EA text-first narration packs, design canon, and any shipped presenter-sibling publication.
- Added `tests/test_verify_next90_m145_fleet_explain_coverage_gate.py` to keep the packet deterministic and to fail when queue/task metadata, package frontier identity, or unexpected artifact fields drift.
- Added the new M145 scripts to Fleet's no-`PYTHONPATH` bootstrap guard in both `tests/test_fleet_script_bootstrap_without_pythonpath.py` and `scripts/verify_script_bootstrap_no_pythonpath.py`, so the explain-coverage gate cannot quietly depend on repo-local module state.
- Tightened the materializer so the local and design queue rows must also keep the assigned `package_id`, `work_task_id`, `repo`, `milestone_id`, and `frontier_id`; future queue-mirror edits can no longer slip past the Fleet closeout gate just because the script still holds the old constants.
- Tightened shipped-sibling validation again so the gate now rejects any closed sibling whose local queue row drifts from the design queue mirror on `title`, `task`, `package_id`, `work_task_id`, `repo`, `milestone_id`, `frontier_id`, `allowed_paths`, or `owned_surfaces`, and it now also rejects worker-local telemetry/helper markers that leak into shipped sibling queue rows or registry work-task evidence.
- Tightened the gate so unshipped optional presenter siblings stay visible as `presenter_optional_fallback: not_shipped` but do not block closeout by themselves; once that optional surface is marked shipped, missing presenter proof still fails closed.
- Tightened the aggregate check computation so shipped sibling queue/registry/design-mirror drift now blocks the matching explain-coverage dimensions directly instead of leaving `coverage_registry_truth` or `deterministic_packet_proof` green while package closeout is blocked underneath.
- Expanded `tests/test_verify_next90_m145_fleet_explain_coverage_gate.py` to 18 regression cases, including shipped sibling queue-title drift, shipped sibling owned-surface drift, and sibling registry helper-proof leakage.
- Tightened the materializer one more step so the Fleet package cannot mark itself complete locally unless the design-owned queue mirror and the canonical successor registry also close `145.6`; future shards cannot reopen the same hole by flipping only the repo-local queue row.
- Reconciled the design-owned queue mirror for the already-shipped `145.1` core packet package and closed the `145.6` Fleet package row in both queue mirrors plus the canonical successor registry, which lets the generated Fleet packet move from honest `blocked` status to honest `pass` status.
- This Fleet slice now fails on package-local queue/registry drift, sibling queue/registry drift, missing coverage-registry packet proof, missing source-anchor posture, missing text-first fallback proof, or a shipped-but-unproven presenter layer instead of letting successor-wave closeout rely on hand-waved sibling status.
