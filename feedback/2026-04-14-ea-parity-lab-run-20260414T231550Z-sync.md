# EA parity-lab sync for shard-11 run `20260414T231550Z`

What changed:

- Resynced `/docker/fleet/docs/chummer5a-oracle/parity_lab_capture_pack.yaml` to the live worker-safe telemetry file for shard-11 run `20260414T231550Z-shard-11`.
- Resynced `/docker/fleet/docs/chummer5a-oracle/veteran_workflow_packs.yaml` to the same run-local telemetry path and current `active_runs_count=11` snapshot.
- Updated `/docker/fleet/docs/chummer5a-oracle/README.md` so the provenance note names the current shard-11 run instead of the previous `20260414T231250Z-shard-11` receipt.

Why this was necessary:

- The EA-owned parity-lab package is meant to be auditable back to the exact worker-safe telemetry inputs for the active run.
- The manifests had drifted to the prior shard-11 run even though the current readiness truth and scope were unchanged.

Verification:

- `python3 /docker/fleet/tests/test_ea_parity_lab_capture_pack.py`

Current boundary:

- Flagship readiness still remains `fail` on `desktop_client` only.
- The unresolved external host-proof tuple is still `avalonia:osx-arm64:macos`.
- No readiness republish was justified because owner-repo proof and external-host receipts did not change in this worker run.
