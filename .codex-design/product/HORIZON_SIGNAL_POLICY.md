# Horizon signal policy

## Purpose

Project Chummer accepts future-capability ideas and public signal, but public signal does not become canonical truth by itself.

## Collection lanes

* `Chummer6`
* Discord
* approved public issue trackers
* `MetaSurvey` for structured collection

`chummer6-design` is not a normal user proposal surface.

## Advisory rule

Signals, votes, survey responses, and community enthusiasm are advisory only.
Canonical horizon posture is published in `chummer6-design`.

## Tool posture

* `MetaSurvey` is the primary structured collection lane.
* `ApproveThis` is the editorial approval bridge for curated intake decisions.
* `Teable` may project or curate signal state, but it is never the system of record.

## Publication rule

When a public idea changes canonical horizon posture, the design update must land in `HORIZONS.md`, the relevant horizon doc, and the milestone or roadmap files in the same change.
