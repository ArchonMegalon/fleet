accounts:
  acct-core-a:
    auth_kind: api_key
    api_key_env: OPENAI_API_KEY
    allowed_models: [gpt-5-mini, gpt-5.4, gpt-5.3-codex]
    health_state: ready
    daily_budget_usd: 30
    monthly_budget_usd: 300
    max_parallel_runs: 1

  acct-ui-a:
    auth_kind: api_key
    api_key_env: OPENAI_API_KEY
    allowed_models: [gpt-5-mini, gpt-5.4]
    health_state: ready
    daily_budget_usd: 20
    monthly_budget_usd: 200
    max_parallel_runs: 1

  acct-hub-a:
    auth_kind: api_key
    api_key_env: OPENAI_API_KEY
    allowed_models: [gpt-5-mini, gpt-5.4]
    health_state: ready
    daily_budget_usd: 20
    monthly_budget_usd: 200
    max_parallel_runs: 1

  acct-ea-a:
    auth_kind: api_key
    api_key_env: OPENAI_API_KEY
    allowed_models: [gpt-5-nano, gpt-5-mini, gpt-5.4]
    health_state: ready
    daily_budget_usd: 10
    monthly_budget_usd: 100
    max_parallel_runs: 1

  acct-ea-fleet:
    auth_kind: api_key
    api_key_env: OPENAI_API_KEY
    # Raw model allowlists are a compatibility fallback; Fleet routes by lane and EA alias first.
    allowed_models: [gpt-5-nano, gpt-5-mini, gpt-5.4, gpt-5.3-codex]
    health_state: ready
    daily_budget_usd: 20
    monthly_budget_usd: 200
    max_parallel_runs: 1
    bridge_name: EA Fleet Worker
    bridge_fallback_accounts: [acct-ea-repair, acct-ea-groundwork, acct-ea-core, acct-ea-survival]
    lane: easy
    codex_model_aliases: [ea-gemini-flash]

  acct-ea-repair:
    auth_kind: api_key
    api_key_env: OPENAI_API_KEY
    allowed_models: [gpt-5-nano, gpt-5-mini, gpt-5.4]
    health_state: ready
    daily_budget_usd: 15
    monthly_budget_usd: 150
    max_parallel_runs: 1
    lane: repair
    codex_model_aliases: [ea-coder-fast]

  acct-ea-groundwork:
    auth_kind: api_key
    api_key_env: OPENAI_API_KEY
    allowed_models: [gpt-5-mini, gpt-5.4]
    health_state: ready
    daily_budget_usd: 10
    monthly_budget_usd: 100
    max_parallel_runs: 1
    lane: groundwork
    codex_model_aliases: [ea-groundwork]

  acct-ea-core:
    auth_kind: api_key
    api_key_env: OPENAI_API_KEY
    allowed_models: [gpt-5-mini, gpt-5.4, gpt-5.3-codex]
    health_state: ready
    daily_budget_usd: 30
    monthly_budget_usd: 300
    max_parallel_runs: 1
    lane: core
    codex_model_aliases: [ea-coder-hard]

  acct-ea-survival:
    auth_kind: api_key
    api_key_env: OPENAI_API_KEY
    # Reserve easy-path account; Codex still launches against ea-coder-survival at runtime.
    allowed_models: [gpt-5-nano, gpt-5-mini]
    health_state: ready
    daily_budget_usd: 5
    monthly_budget_usd: 50
    max_parallel_runs: 1
    lane: survival
    codex_model_aliases: [ea-coder-survival]

  acct-studio-a:
    auth_kind: api_key
    api_key_env: OPENAI_API_KEY
    allowed_models: [gpt-5-mini, gpt-5.4]
    health_state: ready
    daily_budget_usd: 25
    monthly_budget_usd: 250
    max_parallel_runs: 1

  acct-shared-b:
    auth_kind: api_key
    api_key_env: OPENAI_API_KEY
    allowed_models: [gpt-5-nano, gpt-5-mini, gpt-5.4, gpt-5.3-codex]
    health_state: ready
    daily_budget_usd: 50
    monthly_budget_usd: 500
    max_parallel_runs: 1

  acct-chatgpt-core:
    auth_kind: chatgpt_auth_json
    auth_json_file: /run/secrets/acct-chatgpt-core.auth.json
    allowed_models: [gpt-5.3-codex-spark, gpt-5.4, gpt-5.3-codex]
    spark_enabled: true
    health_state: ready
    max_parallel_runs: 1
    # optional hard guardrails for managed environments:
    # forced_login_method: chatgpt
    # forced_chatgpt_workspace_id: 00000000-0000-0000-0000-000000000000

  acct-chatgpt-b:
    auth_kind: chatgpt_auth_json
    auth_json_file: /run/secrets/acct-chatgpt-b.auth.json
    allowed_models: [gpt-5-mini, gpt-5.4]
    spark_enabled: false
    health_state: ready
    max_parallel_runs: 1

  acct-chatgpt-archon:
    auth_kind: chatgpt_auth_json
    auth_json_file: /run/secrets/acct-chatgpt-archon.auth.json
    allowed_models: [gpt-5.3-codex-spark, gpt-5.4, gpt-5.3-codex]
    spark_enabled: true
    health_state: ready
    max_parallel_runs: 1

# For api_key accounts, set either api_key_env or api_key_file.
# If both are set, api_key_env takes precedence.
# Spark-capable aliases must use ChatGPT auth; API-key aliases should not advertise gpt-5.3-codex-spark.
# If multiple aliases share the same ChatGPT auth.json, treat them as one effective human account lane.
# Fleet-wide safe parallelism should track the number of distinct real auth caches, not alias count.
# If a project keeps allow_chatgpt_accounts=true and allow_api_accounts=true in fleet.yaml,
# the broker will use either auth class and still keep Spark on ChatGPT-authenticated aliases only.
