# Black Ledger Faction No-Noise Policy

Public and account-side faction routes must avoid UI noise.

Required:

- every button works
- every link resolves
- public faction pages stay public-safe
- private lore stays on authenticated routes

Forbidden:

- `href="#"`
- `javascript:void`
- placeholder CTAs
- generic unlabeled CTA copy
- public operator/admin leakage
