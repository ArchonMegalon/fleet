# Missed Potential Master Map

## Biggest missed potential areas

### 1. First emotional win

Fix: `Ready for Tonight` mode.

### 2. Campaign continuity

Fix: `NEXUS-PAN` continuity spine.

### 3. Build experimentation

Fix: `ALICE` build ghosts.

### 4. Source-aware trust

Fix: `Knowledge Fabric`.

### 5. House-rule evolution

Fix: `Karma Forge` as a governed package factory.

### 6. World motion

Fix: `Black Ledger` command map, dispatches, and tick replay.

### 7. Session-ready packets

Fix: `JACKPOINT`, `RUNSITE`, and `RUNBOOK PRESS`.

### 8. After-action truth

Fix: `GHOSTWIRE`.

### 9. No-desktop participation

Fix: public web/mobile continuity, participation, and follow lanes.

### 10. Creator/community operating system

Fix: `Creator OS`, `Community Hub`, and `Runner Passport`.

## Closure standard

Each horizon needs:

- one practical daily job
- one visible route
- one proof gate
