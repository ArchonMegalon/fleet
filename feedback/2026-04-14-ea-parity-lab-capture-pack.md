# 2026-04-14 EA parity-lab capture pack

## Shipped

- Added `docs/chummer5a-oracle/parity_lab_capture_pack.yaml` with source-backed screenshot baseline lanes anchored to:
  - Chummer main shell/menu source
  - Master Index utility form
  - Character Roster utility form
  - Chummer5A parity oracle inventory (`tabs`, `workspaceActions`, `desktopControls`)
  - line-level extraction proofs for core menu/workbench landmarks and first-class `Master Index`/`Character Roster` routes
  - tuple-to-baseline mapping for unresolved desktop external host-proof requests
- Added `docs/chummer5a-oracle/veteran_workflow_packs.yaml` with:
  - all required veteran first-minute landmarks and tasks
  - compare-pack mappings for every `FLAGSHIP_PARITY_REGISTRY.yaml` family
  - tuple-level compare packs for all unresolved macOS/Windows promoted desktop tuples
  - explicit desktop non-negotiable assertions (`File` menu, master index, roster, workbench-first startup, no browser-only claim ritual)
- Added `docs/chummer5a-oracle/README.md` as package-level scope/provenance documentation.
- Extended `tests/test_ea_parity_lab_capture_pack.py` to verify line-level source extractions and enforce tuple-map parity with current readiness blockers.

## Still Blocking Full Green

- External host-proof tuples still unresolved in the current published readiness receipt:
  - `avalonia:osx-arm64:macos`
- Local desktop executable-gate blockers also remain open in owner repos across release-channel external-proof contract drift, Linux `blazor-desktop` startup and install-proof failures, and the workflow-execution API surface backlog.

This package intentionally does not fake or bypass those receipts.
