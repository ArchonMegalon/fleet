## EA parity-lab current-run sync

- Resynced `docs/chummer5a-oracle/parity_lab_capture_pack.yaml` and `docs/chummer5a-oracle/veteran_workflow_packs.yaml` to the live shard-11 worker-safe telemetry file for run `20260414T232601Z-shard-11`.
- Updated the embedded telemetry snapshot from `active_runs_count=11` to `active_runs_count=12` while preserving the same live readiness receipt and desktop executable-gate receipt provenance.
- Hardened `tests/test_ea_parity_lab_capture_pack.py` so stale previous-run telemetry paths now fail if they do not match the current `ACTIVE_RUN_HANDOFF.generated.md` run id.
- Ran `python3 /docker/fleet/tests/test_ea_parity_lab_capture_pack.py`; `ran=22 failed=0`.
- Remaining blocker stays external-only for release truth: `avalonia:osx-arm64:macos` still needs fresh macOS host-proof capture and ingestion before `desktop_client` can turn green.
