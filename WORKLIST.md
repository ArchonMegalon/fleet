# Worklist Queue

Purpose: queue the post-client fleet platform work without losing the current flagship closeout overlay.

## Status Keys
- `queued`
- `in_progress`
- `blocked`
- `done`

## Queue
| ID | Status | Priority | Task | Owner | Notes |
|---|---|---|---|---|---|
| WL-300 | done | P0 | Make fleet trust and status publication strictly live-truth so stale aggregate snapshots, poisoned completion receipts, and misleading ETA/currentness states cannot survive into post-client operations. | agent | Shipped: completion audit now keeps receipt failures visible while top-level status and published completion-review frontiers are driven by current live blockers first; aggregate status aliases refresh from live shard truth; operating-profile memory budgets publish explicit currentness. |
| WL-301 | done | P0 | Extract Chummer-specific shard topology, focus heuristics, proof paths, and owner-group routing out of launcher logic into explicit project configuration contracts. | agent | Shipped: `config/projects/fleet.yaml` is now the explicit supervisor contract for shard topology, focus profiles, proof paths, and shard owner/text routing; `run_chummer_design_supervisor.sh` hydrates shard profile/owner/text defaults from that contract while preserving operator env overrides. |
| WL-302 | done | P1 | Add steady-state fleet operating profiles (`maintenance`, `standard`, `burst`) with explicit shard caps, resource budgets, and safe promotion/demotion rules. | agent | Landed in `scripts/chummer_design_supervisor.py`: `--operating-profile`/`CHUMMER_DESIGN_SUPERVISOR_OPERATING_PROFILE` select maintenance, standard, or burst; profile caps and budgets flow through the memory-pressure guard and publish promotion/demotion guidance in the host-memory snapshot. |
| WL-303 | done | P1 | Promote executable gates, parity proofs, and flagship-readiness materializers into first-class orchestrated jobs with freshness windows, retries, and dependency edges. | agent | Shipped: proof lanes now live in `supervisor_contract.proof_jobs` and materialize to `PROOF_ORCHESTRATION.generated.json` with dependency order, retry policy, freshness windows, and output state validation. |
| WL-304 | done | P1 | Build a compact operator surface for shard mix, queue health, proof freshness, account health, resource pressure, and blocked milestones. | agent | Shipped: `/ops/` and `/admin/operator` now render one compact operator surface, `/api/cockpit/operator-surface` exposes the machine-readable contract, and `tests/test_operator_surface.py` proves shard mix, queue health, proof freshness, account health, resource pressure, and blocked frontier/milestone signals are joined. |
| WL-305 | done | P1 | Consolidate restart-safe fleet runtime configuration and validate reboot/recovery behavior so policy survives supervisor restarts and host reboots without tribal knowledge. | agent | Shipped: `config/projects/fleet.yaml` now owns restart-safe topology, state root, resource policy, queue posture, account routing, and worker defaults; `run_chummer_design_supervisor.sh` hydrates those defaults while preserving env overrides; `tests/test_project_policy_contracts.py` validates cold-restart recovery with `CHUMMER_DESIGN_SUPERVISOR_PRINT_RUNTIME_POLICY=1`. |
| WL-306 | done | P0 | Keep fleet queue reliability and anti-starvation strong for long-running 4-month operation: add per-shard debt aging, stalled-worker alerts, and automatic requeue reasons with evidence. | agent | Closed 2026-04-18: operator surface now publishes per-shard debt aging with 15m attention / 60m blocked thresholds, queue health surfaces recent stalled-worker alerts, controller auto-requeues emit signed queue-recovery receipts, and Fleet verify now covers the stale-worker recovery plus queue-health/operator-surface contracts directly. |
| WL-307 | done | P0 | Expand premium burst governance for 4-month window: add explicit guardrails for direct participant burst, separate lanes for managed vs participant burst, and auditable consent/consumption traces. | agent | Closed 2026-04-18: participant contribution receipts now carry consent trace plus sponsor billing attribution and landed/reviewed consumption estimates; operator telemetry separates managed core burst from participant-direct burst and counts receipt/consent drift so burst routing cannot hide missing proof. |
| WL-308 | done | P1 | Stabilize design-doc parity hygiene loop: auto-detect and open queue candidates for recurring mirror-drift clusters (minimum 3 repeated findings -> one queue row). | agent | Closed 2026-04-18: `auditor/app.py` now tracks repeated mirror-drift observations, upgrades the third repeat into one bounded hygiene queue candidate with preserved `source_items` metadata, and keeps queue growth to one synthesized slice instead of reopening one-off mirror refresh work; covered by `tests/test_auditor_synthesis.py`. |
| WL-309 | done | P1 | Add 4-month continuity-liveness monitoring for campaign OS proof fronts: continuity continuity, publication quality, and support-signal freshness under realistic load. | agent | Closed 2026-04-18: `scripts/materialize_campaign_os_continuity_monitor.py` now publishes `CAMPAIGN_OS_CONTINUITY_LIVENESS.generated.json`, proof orchestration treats it as a first-class governance monitor, admin freshness/refresh surfaces keep it live, and the monitor tracks 120-day coverage plus live drift across `PROGRESS_REPORT.generated.json`, `COMPLETION_REVIEW_FRONTIER.generated.yaml`, readiness/journey proofs, and support-signal currentness without pretending the 4-month window is already fully observed. |
| WL-310 | done | P1 | Add daily/weekly fleet evidence reconciliation report for operator decision support: queue health, readiness drift, support backlog, and shell-surface deltas. | agent | Closed 2026-04-18: `/api/admin/queue-reconciliation` now projects queue health, readiness drift, support backlog, and shell-surface deltas; `/ops/` renders the same reconciliation view, including 24h/7d progress deltas against historical snapshots. |
| WL-311 | done | P2 | Improve run attribution and observability: add per-run trace IDs across quartermaster/controller/admin/auditor, and shorten postmortem recovery loops. | agent | Closed 2026-04-19: controller `runs`, auditor `auditor_runs`, admin status/public surfaces, and quartermaster tick/telemetry all carry stable cross-service `trace_id` context with upstream source-trace handoff; admin worker-posture active/recent rows now retain the same trace IDs so postmortem lookup no longer starts from disconnected numeric IDs or raw run inventory. |
| WL-312 | done | P2 | Harden container/host startup resilience for fleet plane during host contention: explicit restart budget, backoff, and bounded startup probes for dashboard/controller/auditor. | agent | Shipped: rebuilder auto-heal now covers `fleet-auditor` in the bounded restart budget alongside controller/dashboard, auditor health now fails if the loop stops producing fresh runs after startup grace, and the dashboard waits on healthy upstream services while probing both `/health` and `/` during startup. |
| WL-313 | done | P2 | Prepare for 4-month scale-up of parallel codexliz execution: add operator policy for multiple `codexliz` lane instances with isolated token/env state and no shared writable auth cache. | agent | Closed 2026-04-19: `scripts/codex-shims/codexliz` auto-assigns and persists lane-local proxy ports when no explicit port is pinned, `tests/test_codexliz_shim.py` covers distinct per-state-dir proxy isolation, README/runtime guidance keeps one token-set per writable lane, and `docs/codexliz_parallel_lane_policy.md` now makes the no-shared-auth-cache operator policy explicit. |
| WL-314 | done | P0 | Add explicit local-canary tests for codexliz transport preflight to prevent `524` blind spots: /v1/models and model-provider sanity checks before accepting queue actions. | agent | Closed 2026-04-18: `scripts/codex-shims/codexliz` now fail-closes on remote plus local-proxy `/v1/models` canaries before delegating to Codex, requires the configured model to exist on that surface, and `tests/test_codexliz_shim.py` covers healthy transport, missing-model drift, and synthetic `524` failures; fleet verify now includes the new shim regression. |
| WL-315 | done | P0 | Add operator visibility for dedicated UI journey tester shard health and routing continuity. | agent | Closed 2026-05-05: operator surface now treats the dedicated tester lane as first-class `shard-14`/`audit_shard` topology, blocks `desktop_client` readiness when it is missing or inactive, and published status-plane materialization now preserves the top-level runtime-healing, support, publish-readiness, and external-proof fields that operator/watchdog evidence depends on. |

## 4-Month Execution Map (Recommended cadence)

### Month 1 (2026-04-18 → 2026-05-16): platform hardening and resilience
- done: WL-306, WL-308

### Month 2 (2026-05-17 → 2026-06-14): burst governance and proof quality
- done: WL-309, WL-310

### Month 3 (2026-06-15 → 2026-07-13): observability and startup control
- done: WL-311
- done: WL-313

### Month 4 (2026-07-14 → 2026-08-11): stabilization and readiness audits
- queued: carry-over remaining from Months 1-3, plus final readiness audit and closure of any open blockers.
