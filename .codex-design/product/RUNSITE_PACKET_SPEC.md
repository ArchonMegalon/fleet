# RUNSITE Packet Spec

RUNSITE packetizes places into:

- site cards
- threat clocks
- entry and exit notes
- GM/player-safe packet splits

Public route:

- `/runsites`
- `/runsites/packs/{packId}.md`
- `/runsites/packs/{packId}.json`

Current state:

- shipped MVP
