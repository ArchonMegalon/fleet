# RUNBOOK PRESS Spec

RUNBOOK PRESS exists to make onboarding:

- printable
- beautiful enough to hand to a human
- bounded by Chummer-owned packet truth

Public route:

- `/runbook`
- `/runbook/primers/{primerId}.md`
- `/runbook/primers/{primerId}.json`

Current state:

- shipped MVP
