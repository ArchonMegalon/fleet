# AppSumo Buy Decision For Dispatch Drafting

Status: draft-only pilot allowed, not publication authority.

Decision rules:

- buy only if export/API is usable
- buy only if public/commercial rights are explicit
- buy only if deletion/privacy posture is acceptable
- buy only if provider names do not leak into public output
- never let external tools publish directly

Current Chummer position:

- external writing tools may assist `DispatchDraft`
- Chummer-owned receipts decide facts
- Chummer-owned gates decide publication
